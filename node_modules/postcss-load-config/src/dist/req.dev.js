"use strict";

// eslint-disable-next-line n/no-deprecated-api
var _require = require('module'),
    createRequire = _require.createRequire,
    createRequireFromPath = _require.createRequireFromPath;

function req(name, rootFile) {
  var create = createRequire || createRequireFromPath;

  var require = create(rootFile);

  return require(name);
}

module.exports = req;