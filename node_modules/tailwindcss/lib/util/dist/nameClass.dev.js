"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});

function _export(target, all) {
  for (var name in all) {
    Object.defineProperty(target, name, {
      enumerable: true,
      get: all[name]
    });
  }
}

_export(exports, {
  asClass: function asClass() {
    return _asClass;
  },
  "default": function _default() {
    return nameClass;
  },
  formatClass: function formatClass() {
    return _formatClass;
  }
});

var _escapeClassName =
/*#__PURE__*/
_interop_require_default(require("./escapeClassName"));

var _escapeCommas =
/*#__PURE__*/
_interop_require_default(require("./escapeCommas"));

function _interop_require_default(obj) {
  return obj && obj.__esModule ? obj : {
    "default": obj
  };
}

function _asClass(name) {
  return (0, _escapeCommas["default"])(".".concat((0, _escapeClassName["default"])(name)));
}

function nameClass(classPrefix, key) {
  return _asClass(_formatClass(classPrefix, key));
}

function _formatClass(classPrefix, key) {
  if (key === "DEFAULT") {
    return classPrefix;
  }

  if (key === "-" || key === "-DEFAULT") {
    return "-".concat(classPrefix);
  }

  if (key.startsWith("-")) {
    return "-".concat(classPrefix).concat(key);
  }

  if (key.startsWith("/")) {
    return "".concat(classPrefix).concat(key);
  }

  return "".concat(classPrefix, "-").concat(key);
}