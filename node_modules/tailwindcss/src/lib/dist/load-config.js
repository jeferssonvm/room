"use strict";
exports.__esModule = true;
exports.loadConfig = void 0;
var jiti_1 = require("jiti");
var sucrase_1 = require("sucrase");
var jiti = null;
function lazyJiti() {
    return (jiti !== null && jiti !== void 0 ? jiti : (jiti = jiti_1["default"](__filename, {
        interopDefault: true,
        transform: function (opts) {
            return sucrase_1.transform(opts.source, {
                transforms: ['typescript', 'imports']
            });
        }
    })));
}
function loadConfig(path) {
    var _a;
    var config = (function () {
        try {
            return path ? require(path) : {};
        }
        catch (_a) {
            return lazyJiti()(path);
        }
    })();
    return (_a = config["default"]) !== null && _a !== void 0 ? _a : config;
}
exports.loadConfig = loadConfig;
